import React from 'react';
import PropTypes from 'prop-types';

const ImmaComponent = ( props ) =>{
    return (
        <div>TESTS</div>
    );
};

ImmaComponent.propTypes = {};

export default ImmaComponent;
